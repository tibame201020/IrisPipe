---
title: "??????"
---

IrisPipe ? **PR ????** ???????? benchmark ???

- **k6 Benchmark**?API/runtime latency?1k / 10k / 100k data-volume?failure semantics??? PostgreSQL ? H2?
- **Backend K6 Checks**???? Spring Boot backend ?? 10 ? regression suites?
- **k6 Deep Volume Benchmark**?scheduled/manual ??????????
- **Full RDBMS Matrix**?? source ?? 6 ? workflow??? H2?PostgreSQL?MySQL?MariaDB?SQL Server?Oracle?????? 6 sources ? 6 destinations ? JOB/CHUNK ? 100K/1M/10M/20M/50M?? **360 cases**?

[![k6 Benchmark](https://github.com/tibame201020/IrisPipe/actions/workflows/k6-benchmark.yml/badge.svg?branch=main)](https://github.com/tibame201020/IrisPipe/actions/workflows/k6-benchmark.yml)
[![Backend K6 Checks](https://github.com/tibame201020/IrisPipe/actions/workflows/backend-k6-check.yml/badge.svg?branch=main)](https://github.com/tibame201020/IrisPipe/actions/workflows/backend-k6-check.yml)
[![k6 Deep Volume Benchmark](https://github.com/tibame201020/IrisPipe/actions/workflows/k6-deep-volume.yml/badge.svg?branch=main)](https://github.com/tibame201020/IrisPipe/actions/workflows/k6-deep-volume.yml)

---

{{< include ../_generated/performance-results-zh.md >}}

{{< include ../_generated/deep-performance-results-zh.md >}}

---

## ?? k6 Regression Status

| Suite | Status | ???? |
|---|---:|---|
| Config | PASS | Create / update / import / delete guard / uniqueness |
| Workspace | PASS | Workspace scoping?default fallback?cross-workspace isolation |
| Pipeline Core | PASS | Execute flow?stage barrier?run lifecycle |
| Pipeline Resume | PASS | failed / stopped run resume?skip completed stages |
| Pipeline Stop | PASS | Cooperative stop?STOPPING ? STOPPED |
| Operator Safety | PASS | double-execute guard??? resume/delete ?? |
| Observability | PASS | Prometheus metrics?actuator health?overview API |
| Pipeline Rerun | PASS | ? frozen snapshot ??? logical run |
| Control Loop | PASS | execute ? fail ? resume ? stop ? rerun |
| Runtime | PASS | watermark?lineage?attempts?snapshot isolation |

---

## Benchmark ?????

Data-volume benchmark ???? Spring Batch + JDBC???????????????

1. ?? PR/main gate?H2 ? H2 JOB/CHUNK?1k / 10k / 100k?
2. Failure semantics???????? duplicate key????? JOB / CHUNK rollback ???
3. Cross-database correctness?destination row count ????????????? pipeline `COMPLETED`?
4. Full matrix?H2 / PostgreSQL / MySQL / MariaDB / SQL Server / Oracle ?? source ????? destination ???
5. Capacity ladder?JOB / CHUNK ?? 100K / 1M / 10M / 20M / 50M?

?????? GitHub-hosted runner ????? regression benchmark?**?? production-capacity claim**?

---

## JOB / CHUNK?Transaction Boundary

??? `batchSize`????????

```{mermaid}
flowchart LR
  subgraph JOB["JOB ? 50M rows"]
    J1["JDBC batches?5,000 rows"] --> J2["?? Job = 1 transaction"] --> J3["???50M ?? rollback"]
  end
  subgraph CHUNK["CHUNK ? 50M rows"]
    C1["10,000 chunks ? 5,000 rows"] --> C2["10,000 ??? commit"] --> C3["???? rollback ?? chunk"]
  end
```

| CI ?? | `fetchSize` / `batchSize` | JOB ???? | CHUNK ???? |
|---|---:|---|---|
| ?? PR/main benchmark | 1,000 rows | ?? Job ?? destination transaction | ? 1,000 rows commit |
| Deep/full benchmark | 5,000 rows | ?? Job ?? destination transaction | ? 5,000 rows commit |

| Source rows | CHUNK transaction groups @ 5,000 | JOB transaction groups |
|---:|---:|---:|
| 100K | 20 | 1 |
| 1M | 200 | 1 |
| 10M | 2,000 | 1 |
| 20M | 4,000 | 1 |
| 50M | 10,000 | 1 |

10k failure case ?????????????? duplicate key?batch=1,000 ??**JOB** ? rollback ??????????????? **1 row**?**CHUNK** ???? 9 ?? commit chunks?9,000 rows??? rollback ? 10 ??? chunk??? destination ??? **9,001 rows**?

---

## Benchmark ????

`duration_ms` / `rows_per_second` ???? **IrisPipe migration execution**??????????????

```{mermaid}
flowchart LR
  A["DB/container startup"] --> B["Source seed"] --> C["Backend startup"] --> D["Pipeline config"] --> E["START TIMER"] --> F["POST /sync-pipeline\nSpring Batch + JDBC + commits"] --> G["STOP TIMER"] --> H["Destination COUNT ??"] --> I["Publish report"]
```

**????** ????? `/api/v1/sync-pipeline` ????? completed/failed response ???

**????** DB/container ???source initial data ???backend startup/readiness?pipeline config ?????? destination `COUNT(*)`?artifact aggregation?Pages deployment?

????? rows/s ??????????Spring Batch / JDBC / transaction ??????????????????

---

## Test Strategy

k6 ? IrisPipe ??? synthetic load generation???? **integration-level correctness**??? benchmark ????? backend????? pipeline??? JDBC migration??? runtime status???? destination ?????

External DB ? source data ? **? timer ?????? DB ???**?? engine pair ???? source / destination containers?? engine pair ???? engine instance ????? source / destination tables?

---

## CI / Benchmark Trigger

50M ????????? PR merge???????? `main` GitHub Pages deployment?

```{mermaid}
flowchart TD
  PR["PR / main push"] --> G["Unit tests + regular k6 gates"]
  G --> P["????? benchmark JSON ?? Quarto"]

  T["tag?benchmark-full-*"] --> H2["Source H2 ? 60 cases"]
  T --> PG["Source PostgreSQL ? 60 cases"]
  T --> MY["Source MySQL ? 60 cases"]
  T --> MA["Source MariaDB ? 60 cases"]
  T --> MS["Source SQL Server ? 60 cases"]
  T --> OR["Source Oracle ? 60 cases"]

  M["Manual?Full RDBMS Benchmark - All Sources"] --> H2
  M --> PG
  M --> MY
  M --> MA
  M --> MS
  M --> OR

  H2 --> R["?? merge ? retained deep-performance-results.json"]
  PG --> R
  MY --> R
  MA --> R
  MS --> R
  OR --> R
  R --> D["repository_dispatch ? GitHub Pages"]
```

`benchmark-postgres-*` ?? source-specific tag ???? source?`benchmark-full-*` ??????? 6 ? source workflows?

?????? `(db_pair, atomic_level, mode, row_count, batch_size)` ?? merge key??? merge ? `main` **??????? deep results**??? source benchmark ????????????????? cases?

---

## Benchmark Methodology

1. `mvn package` ?? backend unit tests?
2. PR/main gate ?????10M+ ?? tag/manual/scheduled benchmark ???
3. Full matrix ?? `batchSize=5,000`??? 100K / 1M / 10M / 20M / 50M?
4. Source seed ? migration timer ??????k6 ?????? 50M initial data?
5. External destination ? row count ? timer ?????????????????
6. ??? merge ? retained history?????? source/destination ???????????
