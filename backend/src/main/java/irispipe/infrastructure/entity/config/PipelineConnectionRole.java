package irispipe.infrastructure.entity.config;

/**
 * Identifies whether one persisted job connection is the source or destination
 * side.
 */
public enum PipelineConnectionRole {
    SOURCE,
    DEST
}
