# test-config-invalid-format.yml
- jobName: k6_test_config_invalid
  # Missing 'executions' list
  setting:
    fetchSize: 100
    batchSize: 100
    atomicLevel: JOB
  database:
    # Missing Dest
    source:
      driver: org.h2.Driver
      url: jdbc:h2:./h2data/data
      username: sa
      password: "sa"
